# Optional: only needed to rerun API-based generation or blind judging.
# The training and temporary demo notebooks require no API credential.
OPENAI_API_KEY=replace_with_your_compatible_endpoint_key
OPENAI_BASE_URL=https://your-compatible-endpoint.example/v1
OPENAI_JUDGE_MODEL=replace_with_your_judge_model
